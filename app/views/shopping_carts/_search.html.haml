Search for products

=form_tag event_shopping_cart_search_path(@event, @shopping_cart), method: 'get' do
  =text_field_tag :search, params[:search], :id => 'search_field'
  =submit_tag "Search", :name => nil

