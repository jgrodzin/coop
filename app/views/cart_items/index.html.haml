Current shopping cart!

%table{:cellpadding => "0", :cellspacing => "0"}
  %tbody
    %tr
      %th Product name
    - @shopping_cart.cart_items.map(&:product).map do |product|
      %tr
        %td= product.name
