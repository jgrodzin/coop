- if current_member.admin?
  .add-product-link
    %button
      %h5= link_to "Add Vendor", new_vendor_path

%h2 Vendors

%article
  %table{ cellspacing: "0", cellpadding: "0"}
    %tbody
      %tr
        %th Name
        %th Rep
        %th Category
        %th Address
        %th Payment Method

    - @vendors.each do |vendor|
      %tr
        %td= link_to("#{vendor.name}", vendor_path(vendor))
        %td= vendor.rep
        %td= vendor.category
        %td= vendor.address
        %td= vendor.payment
